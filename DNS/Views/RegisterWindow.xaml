<Window x:Class="DNS.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="DNS - Регистрация" Height="670" Width="400"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="37*"/>
            <ColumnDefinition Width="43*"/>
        </Grid.ColumnDefinitions>
        <materialDesign:Card Padding="32" Margin="16,0,16,0" VerticalAlignment="Center" Grid.ColumnSpan="2">
            <StackPanel>
                <Image Source="/Images/logo.png" Width="150" Height="60" Margin="0,0,0,32"/>

                <TextBlock Text="Регистрация"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,32"/>

                <TextBox x:Name="UsernameBox"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         materialDesign:HintAssist.Hint="Имя пользователя"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Margin="0,0,0,16"/>

                <TextBox x:Name="EmailBox"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         materialDesign:HintAssist.Hint="Email"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Margin="0,0,0,16"/>

                <PasswordBox x:Name="PasswordBox"
                            Style="{StaticResource MaterialDesignFilledPasswordBox}"
                            materialDesign:HintAssist.Hint="Пароль"
                            Margin="0,0,0,16"/>

                <PasswordBox x:Name="ConfirmPasswordBox"
                            Style="{StaticResource MaterialDesignFilledPasswordBox}"
                            materialDesign:HintAssist.Hint="Подтвердите пароль"
                            Margin="0,0,0,32"/>

                <Button Content="Зарегистрироваться"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="RegisterButton_Click"
                        Margin="0,0,0,16"/>

                <Button Content="Уже есть аккаунт? Войти"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Click="LoginButton_Click"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
